# Core API
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Image processing
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.0.0

# Fish detection
ultralytics==8.3.40

# Deep learning (CPU-only build — much smaller than full CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# ── Storage backends (uncomment the one you need) ────────────────────────────
# Default: in-memory (no extra package needed, STORAGE_BACKEND=memory)

# Cloudinary (STORAGE_BACKEND=cloudinary, set CLOUDINARY_URL env var)
# Sign up free at https://cloudinary.com — 25 GB/month free
# cloudinary==1.41.0

# Supabase (STORAGE_BACKEND=supabase, set SUPABASE_URL + SUPABASE_KEY env vars)
# Sign up free at https://supabase.com — 1 GB storage free
# supabase==2.9.1

# ── Optional: Real-ESRGAN upscaling ─────────────────────────────────────────
# Installed separately in Dockerfile with --no-build-isolation
# (basicsr uses a legacy setup.py that conflicts with newer setuptools+CUDA)
