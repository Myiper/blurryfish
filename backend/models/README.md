# Place your model weight files here
#
# Required files:
#   denoising_unet.pth    — custom U-Net denoising weights
#   best.pt               — YOLOv8 fish detector weights
#   RealESRGAN_x4plus.pth — Real-ESRGAN upscaler (optional)
#
# These files are excluded from git via .gitignore.
# Upload them directly to the Render service using the Render disk or
# host them on Hugging Face / GDrive and download at build time.
